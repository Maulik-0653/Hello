name: hello

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Clone repository
        run: git clone https://github.com/Maulik-0653/Action

      - name: list files
        run: ls -la
           